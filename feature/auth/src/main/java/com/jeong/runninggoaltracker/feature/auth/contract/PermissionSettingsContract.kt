package com.jeong.runninggoaltracker.feature.auth.contract

import android.provider.Settings

object PermissionSettingsContract {
    const val PACKAGE_URI_SCHEME = "package"
    const val ACTION_APPLICATION_DETAILS_SETTINGS = Settings.ACTION_APPLICATION_DETAILS_SETTINGS
}
